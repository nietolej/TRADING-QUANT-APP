db:
  path: "data/trading_quant.db"

data:
  binance:
    supported_timeframes:
      - "1m"
      - "5m"
      - "15m"
      - "1h"
      - "4h"
      - "1d"
  onchain:
    cache_ttl_hours: 24

backtest:
  commission_pct: 0.1
  slippage_pct: 0.05
  default_initial_capital: 10000
